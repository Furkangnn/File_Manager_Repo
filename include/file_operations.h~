#ifndef FILE_OPERATIONS_H
#define FILE_OPERATIONS_H

void removeFile(const char *path);  // Dosya silme fonksiyonu prototipi
void copyFile(const char *source, const char *destination);  // Dosya kopyalama
void displayFile(const char *path);  // Dosya içeriğini gösterme
void createFile(const char *filename)
// Diğer fonksiyon prototipleri...

#endif

